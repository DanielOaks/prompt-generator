{
  "name": "prompts",
  "version": "1.0.0",
  "description": "A character, location, and scene generator for illustrations, short stories, etc.",
  "main": "src/index.js",
  "repository": "git@github.com:DanielOaks/prompt-generator.git",
  "author": "Daniel Oakley <daniel@danieloaks.net>",
  "scripts": {
    "build": "npm run-script clean && webpack --progress --profile --colors --mode=production",
    "serve": "npm run-script clean && webpack --progress --profile --colors --mode=development",
    "clean": "rimraf ./_site ./assets"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  }
}
